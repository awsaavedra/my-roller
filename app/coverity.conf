{
    "type": "Coverity configuration",
    "format_version": 1,
    "format_minor_version": 7,
    
    "settings": {
        "cov_run_desktop": {
            "build_cmd": ["make", "all"], // replace `make all` with the build command for your project
            "clean_cmd": ["make", "clean"] // replace `make clean` with the clean command for your project
        },
        "ide": {
            "build_strategy": "CUSTOM" // this line is important for Code Sight
        }
    }
}